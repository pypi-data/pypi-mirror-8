.. contents::

Introduction
============

A simple slider (fader) one can enable and show images based on a folder or a keyword (tag).
It will also serve different sizes of the images based on urls (add zetterk.mobiletheming).