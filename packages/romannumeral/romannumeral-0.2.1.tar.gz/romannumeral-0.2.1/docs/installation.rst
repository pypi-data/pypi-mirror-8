============
Installation
============

At the command line::

    $ easy_install romannumeral

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv romannumeral
    $ pip install romannumeral