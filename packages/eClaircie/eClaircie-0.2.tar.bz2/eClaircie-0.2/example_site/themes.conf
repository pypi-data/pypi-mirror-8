
# This list allows to specify per-category/document themes (overriding the default theme in conf.py).
# 
# Note: per-category themes are specified in a separate config file, in order to permit to change them
# without having Sphinx rebuild the entire website.

ec_multiple_themes = {
  "themes/champi" : "champi",
  "themes/green_roses" : "green_roses",
  "themes/balazar" : "balazar",
# "category/subcategory" : "theme",
}
