#!/bin/bash

# XXX lsb functions ?
TERM=linux
SCRIPT=$(basename $0)

shift

print_hook() {
	echo -e "\e[1;32m${SCRIPT}: $*\e[0;0m"
}
eprint_hook() {
	echo -e "\e[1;31m${SCRIPT}: $*\e[0;0m"
}
wprint_hook() {
	echo -e "\e[1;33m${SCRIPT}: $*\e[0;0m"
}
