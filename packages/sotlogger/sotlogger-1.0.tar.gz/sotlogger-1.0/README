## Database schema ##
#CREATE KEYSPACE sot WITH replication = {'class':'SimpleStrategy', 'replication_factor':3};
#CREATE TABLE sot.events (id bigint, event_data blob, component varchar, component_name varchar, created_date timestamp, PRIMARY KEY (id, created_date));
#CREATE TABLE sot.eventlogs (event_id bigint, message blob, component varchar, component_name varchar, log_level int, created_date timestamp, PRIMARY KEY (event_id, created_date));