Sale shop add a required field in sales. If you install sale shop when you have
sales you need before install read and execute some SQL commands.

PSQL command:

# alter table sale_sale add column shop int;

Install sale shop module and create new shop (ID = 1)

PSQL command:

# update sale_sale set shop = 1;
# alter table sale_sale alter column shop set not null;
