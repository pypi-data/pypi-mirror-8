========
 README
========

``brood-json-schemas`` is a simple package that contains the
JSON-schemas for ``brood`` so that they may be reused across projects,
for example for testing that ``hatcher`` submits valid JSON to
``brood``.
