{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Egg Metadata",
    "description": "Uploaded metadata of a brood egg",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "sha256": {
            "description": "The SHA256 hash of the runtime file.",
            "type": "string"
        }
    },
    "required": ["sha256"]
}
