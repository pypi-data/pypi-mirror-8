{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Create Team",
    "description": "Team metadata",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "name": {
            "description": "The name of the new team.",
            "type": "string"
        },
        "group": {
            "description": "The name of the group providing the team's permissions.",
            "type": "string"
        },
        "repositories": {
            "description": "The repositories to which this team will provide access.  These repositories must be owned by the same organization that owns the new team.",
            "type": "array",
            "items": {
                "type": "string"
            },
            "minItems": 0,
            "uniqueItems": true
        }
    },
    "required": ["name", "group"]
}
