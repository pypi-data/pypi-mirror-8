{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Create Platform",
    "description": "Brood Platform Specification",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "name": {
            "description": "The name of the operating system.",
            "type": "string",
            "maxLength": 64
        },
        "arch": {
            "description": "The name of the underlying architecture.",
            "type": "string",
            "maxLength": 64
        }
    },
    "required": ["name", "arch"]
}
