project: apnsclient
license: BSD
auth   : David Wu

apnsclient是为接入apnsagent开发的客户端


代码范例：
from apnsclient import PushClient
p = PushClient(app_key, app_secret, 'http://localhost:8888')
p.push(1, 'hello', 88, 'default', {'a':1}) 


参数：
- PushClient
	初始化参数：
		key  分配的app key
		secret  分配的app key
		host    指向的apnsagent web服务器 默认是：http://apns.sutui.me

- push 方法
  参数：
  	user_id   用户id
  	message   推送内容
  	badge     未读数，默认为0
  	sound     提示声音，默认为default，留空为不响铃
  	custom    携带数据，要求dict格式，默认为{}
  	

工程依赖
- apnsagent
- requests
- simplejson

