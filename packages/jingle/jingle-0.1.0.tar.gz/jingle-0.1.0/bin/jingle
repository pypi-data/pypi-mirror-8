#!/usr/bin/env python

import os
import sys

from jingle import CLI

if __name__ == "__main__":
    try:
        CLI(sys.argv[1:]).run()
    except KeyboardInterrupt:
        sys.stderr.write("Interrupted...\n")
        exit(130)
