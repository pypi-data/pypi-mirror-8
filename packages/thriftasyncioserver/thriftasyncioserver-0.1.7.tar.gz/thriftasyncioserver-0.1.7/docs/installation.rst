============
Installation
============

At the command line::

    $ easy_install thriftasyncioserver

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv thriftasyncioserver
    $ pip install thriftasyncioserver