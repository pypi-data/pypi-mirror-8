from distutils.core import setup

setup(name         = "NesterSJP",
      version      = "1.0.0",
      py_modules   = ["NesterSJP"],
      author       = "SJP",
      description  = "A simple function which prints the items of a (nested) list")

