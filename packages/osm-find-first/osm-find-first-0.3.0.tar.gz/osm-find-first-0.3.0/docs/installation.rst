============
Installation
============

At the command line::

    $ easy_install osm-find-first

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv osm-find-first
    $ pip install osm-find-first