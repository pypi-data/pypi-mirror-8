# Keep spacing

LoadPlugin postgresql

<Plugin "postgresql">
    <Database "{{ postgresql.name }}">
        Host "localhost"
        User "{{ postgresql.username }}"
        Password "{{ postgresql.password }}"
    </Database>
</Plugin>

# Keep spacing
