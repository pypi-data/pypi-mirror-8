# Keep spacing

LoadPlugin nginx

<Plugin "nginx">
    URL "{{ nginx.url }}"
    {% if nginx.username %}
        Username "{{ nginx.username }}"
        Password "{{ nginx.password }}"
    {% endif %}
    VerifyHost false
    VerifyPeer false
</Plugin>

# Keep spacing
